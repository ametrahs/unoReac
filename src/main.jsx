// import { StrictMode } from 'react'
// import { createRoot } from 'react-dom/client'
// import './index.css'
// import App from './App.jsx'

// createRoot(document.getElementById('root')).render(
//   <StrictMode>
//     <App />
//   </StrictMode>,
// )

/* import {createRoot} from "react-dom/client"
const root = createRoot(document.getElementById('root'))

root.render(
  <h1> hello react</h1>
) */
// ** scrimba day-one
// *** challenge-one
/* import {createRoot} from "react-dom/client"

const root = createRoot(document.getElementById('root'))
root.render(
  <h1 style ={{color : "green"}}>you will be baletsega female in z world</h1>
) */
// *** challenge-two
/* import { createRoot } from "react-dom/client"

const root = createRoot(document.getElementById('root'))
root.render(
  <ul>
    <li>hi winner!</li>
    <li>hi winner!</li>
    <li>hi winner!</li>
  </ul>
) */
// *** challenge-three
